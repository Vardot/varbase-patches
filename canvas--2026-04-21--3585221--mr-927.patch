diff --git a/src/Entity/VersionedConfigEntityBase.php b/src/Entity/VersionedConfigEntityBase.php
index b403ff99..43ea4a6f 100644
--- a/src/Entity/VersionedConfigEntityBase.php
+++ b/src/Entity/VersionedConfigEntityBase.php
@@ -52,7 +52,16 @@ abstract class VersionedConfigEntityBase extends ConfigEntityBase implements Ver
 
   public function loadVersion(string $version): static {
     if ($version !== $this->loadedVersion) {
-      $this->assertVersionExists($version);
+      if ($version !== $this->active_version && !\array_key_exists($version, $this->versioned_properties)) {
+        \Drupal::logger('canvas')->warning('Component version `@requested` not found for @entity_type `@id`, falling back to active version `@active`.', [
+          '@requested' => $version,
+          '@entity_type' => $this->getEntityTypeId(),
+          '@id' => $this->id(),
+          '@active' => $this->active_version,
+        ]);
+        $this->loadedVersion = $this->active_version;
+        return $this;
+      }
       $this->loadedVersion = $version;
     }
     return $this;
