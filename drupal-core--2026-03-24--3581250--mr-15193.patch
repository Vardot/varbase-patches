diff --git a/core/lib/Drupal/Core/Config/ConfigInstaller.php b/core/lib/Drupal/Core/Config/ConfigInstaller.php
index 8eff8f21554..2eb41234615 100644
--- a/core/lib/Drupal/Core/Config/ConfigInstaller.php
+++ b/core/lib/Drupal/Core/Config/ConfigInstaller.php
@@ -420,7 +420,15 @@ protected function createConfiguration($collection, array $config_to_create) {
         // creation. Updates of such configuration are allowed.
         if ($this->getActiveStorages($collection)->exists($name)) {
           $entity = $entity_storage->load($id);
-          $entity = $entity_storage->updateFromStorageRecord($entity, $new_config->get());
+          try {
+            $entity = $entity_storage->updateFromStorageRecord($entity, $new_config->get());
+          }
+          catch (\LogicException $e) {
+            // Skip entities that cannot be updated from storage records, such
+            // as versioned config entities (e.g., canvas content templates)
+            // that detect version history wipes during recipe installation.
+            continue;
+          }
         }
         else {
           $entity = $entity_storage->createFromStorageRecord($new_config->get());
@@ -430,6 +438,14 @@ protected function createConfiguration($collection, array $config_to_create) {
           if ($id !== $entity->id()) {
             throw new \LogicException(sprintf('The configuration name "%s" does not match the ID "%s"', $name, $entity->id()));
           }
+          // Clear cached field definitions after saving field-related config
+          // entities. This ensures that subsequent config entities being
+          // created in the same loop iteration can discover newly created
+          // field storages and field instances. Without this, stale cached
+          // definitions cause FieldExceptions during Recipe installation.
+          if ($entity_type === 'field_storage_config' || $entity_type === 'field_config') {
+            \Drupal::service('entity_field.manager')->clearCachedFieldDefinitions();
+          }
         }
       }
       else {
diff --git a/core/modules/field/src/Entity/FieldConfig.php b/core/modules/field/src/Entity/FieldConfig.php
index c27ca0ef272..0344f33f778 100644
--- a/core/modules/field/src/Entity/FieldConfig.php
+++ b/core/modules/field/src/Entity/FieldConfig.php
@@ -314,6 +314,18 @@ public function getFieldStorageDefinition() {
         }
       }
 
+      // Fallback: try loading field storage directly from config entity
+      // storage. This handles cache timing issues during Recipe installation
+      // where field storage configs have been saved to the database but
+      // EntityFieldManager's cached definitions have not been updated yet.
+      if (!$field_storage_definition) {
+        \Drupal::service('entity_field.manager')->clearCachedFieldDefinitions();
+        $field_storage_definitions = \Drupal::service('entity_field.manager')->getFieldStorageDefinitions($this->entity_type);
+        if (isset($field_storage_definitions[$this->field_name])) {
+          $field_storage_definition = $field_storage_definitions[$this->field_name];
+        }
+      }
+
       if (!$field_storage_definition) {
         throw new FieldException("Attempted to create, modify or delete an instance of field with name {$this->field_name} on entity type {$this->entity_type} when the field storage does not exist.");
       }
