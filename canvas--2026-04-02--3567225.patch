diff -ruN a/packages/types/src/DrupalSettings.ts b/packages/types/src/DrupalSettings.ts
--- a/packages/types/src/DrupalSettings.ts	2026-04-02 06:14:28.405468349 +0300
+++ b/packages/types/src/DrupalSettings.ts	2026-04-02 06:14:34.892255165 +0300
@@ -40,6 +40,7 @@
     personalizationExtensionAvailable: boolean;
     // ⚠️ This is highly experimental and *will* be refactored.
     canvasAiMaxFileSize: number;
+    hidePageDataPanel?: boolean;
   };
   canvasData: {
     v0: {
diff -ruN a/src/Controller/ApiLayoutController.php b/src/Controller/ApiLayoutController.php
--- a/src/Controller/ApiLayoutController.php	2026-04-02 06:14:28.410154699 +0300
+++ b/src/Controller/ApiLayoutController.php	2026-04-02 06:14:50.622499559 +0300
@@ -307,7 +307,11 @@
 
     // Update the entity & auto-save it. We might be updating a component
     // instance version aside of the model itself.
-    $this->updateComponentInstance($entity_to_patch, $componentInstanceUuid, $version, $model, $preview_entity);
+    // For per-entity canvas layouts, the entity itself is the host entity for
+    // field linking (EntityFieldPropSource). For ContentTemplate editing, the
+    // preview entity serves as the host.
+    $host_entity = $preview_entity ?? ($entity instanceof FieldableEntityInterface ? $entity : NULL);
+    $this->updateComponentInstance($entity_to_patch, $componentInstanceUuid, $version, $model, $host_entity);
     $this->autoSaveManager->saveEntity($entity_to_patch, $clientInstanceId);
 
     // Inform the UI of the updated reality.
diff -ruN a/src/Plugin/Canvas/ComponentSource/GeneratedFieldExplicitInputUxComponentSourceBase.php b/src/Plugin/Canvas/ComponentSource/GeneratedFieldExplicitInputUxComponentSourceBase.php
--- a/src/Plugin/Canvas/ComponentSource/GeneratedFieldExplicitInputUxComponentSourceBase.php	2026-04-02 06:14:28.422192793 +0300
+++ b/src/Plugin/Canvas/ComponentSource/GeneratedFieldExplicitInputUxComponentSourceBase.php	2026-04-02 06:15:13.995864214 +0300
@@ -747,13 +747,20 @@
     // Component config entity being created!)
 
     // Get suggested non-static prop sources for component instances on
-    // content templates.
+    // content templates and per-entity canvas layouts.
     $suggestions = NULL;
+    $entity_data_definition_for_linking = NULL;
     if ($entity instanceof ContentTemplate) {
+      $entity_data_definition_for_linking = $entity->getTargetEntityDataDefinition();
+    }
+    elseif ($entity instanceof \Drupal\Core\Entity\FieldableEntityInterface && $entity->getEntityTypeId() === 'node') {
+      $entity_data_definition_for_linking = \Drupal\Core\Entity\TypedData\EntityDataDefinition::create('node', $entity->bundle());
+    }
+    if ($entity_data_definition_for_linking !== NULL) {
       $suggestions = PropSourceSuggester::structureSuggestionsForHierarchicalResponse($this->propSourceSuggester->suggest(
         $this->getSourceSpecificComponentId(),
         $this->getMetadata(),
-        $entity->getTargetEntityDataDefinition(),
+        $entity_data_definition_for_linking,
       ));
     }
 
@@ -800,7 +807,7 @@
       $form[$sdc_prop_name] = $source->formTemporaryRemoveThisExclamationExclamationExclamation($widget, $sdc_prop_name, $is_required, $entity_object_for_field_widget, $form, $form_state);
       $form[$sdc_prop_name]['#disabled'] = $disabled;
 
-      if ($entity instanceof ContentTemplate) {
+      if ($entity_data_definition_for_linking !== NULL) {
         $could_use_dynamic_prop_source = !empty($suggestions[$sdc_prop_name]);
 
         // If the prop is already linked, replace the widget entirely. The
@@ -814,7 +821,7 @@
             '#sdc_prop_name' => $sdc_prop_name,
             '#sdc_prop_label' => $label,
             '#linked_prop_source' => $linked_prop_source,
-            '#entity_data_definition' => $entity->getTargetEntityDataDefinition(),
+            '#entity_data_definition' => $entity_data_definition_for_linking,
             '#field_link_suggestions' => $suggestions[$sdc_prop_name],
             '#description' => $component_schema['properties'][$sdc_prop_name]['description'] ?? NULL,
             '#is_required' => $is_required,
@@ -847,7 +854,7 @@
       }
     }
     $form['#attached']['canvas-transforms'] = $transforms;
-    if ($entity instanceof ContentTemplate) {
+    if ($entity_data_definition_for_linking !== NULL) {
       $form['#after_build'][] = [static::class, 'moveSuggestionsToLabel'];
     }
     return $form;
diff -ruN a/src/Storage/ComponentTreeLoader.php b/src/Storage/ComponentTreeLoader.php
--- a/src/Storage/ComponentTreeLoader.php	2026-04-02 06:14:28.428760007 +0300
+++ b/src/Storage/ComponentTreeLoader.php	2026-04-02 06:15:25.129038513 +0300
@@ -15,11 +15,11 @@
 /**
  * Handles loading a component tree from entities.
  */
-final class ComponentTreeLoader {
+class ComponentTreeLoader {
 
   public function __construct(
-    private readonly EntityFieldManagerInterface $entityFieldManager,
-    private readonly ModuleHandlerInterface $moduleHandler,
+    protected readonly EntityFieldManagerInterface $entityFieldManager,
+    protected readonly ModuleHandlerInterface $moduleHandler,
   ) {}
 
   /**
diff -ruN a/ui/src/components/form/components/drupal/LinkedFieldBox.tsx b/ui/src/components/form/components/drupal/LinkedFieldBox.tsx
--- a/ui/src/components/form/components/drupal/LinkedFieldBox.tsx	2026-04-02 06:14:28.438526324 +0300
+++ b/ui/src/components/form/components/drupal/LinkedFieldBox.tsx	2026-04-02 06:15:50.687440088 +0300
@@ -11,7 +11,7 @@
 } from '@/features/layout/layoutModelSlice';
 import { findComponentByUuid } from '@/features/layout/layoutUtils';
 import {
-  EditorFrameContext,
+  selectEditorFrameContext,
   selectSelectedComponentUuid,
 } from '@/features/ui/uiSlice';
 import { useGetComponentsQuery } from '@/services/componentAndLayout';
@@ -61,6 +61,7 @@
   const [selectedComponentType, version] = (
     node ? (node.type as string) : 'noop'
   ).split('@');
+  const editorFrameContext = useAppSelector(selectEditorFrameContext);
   const [patchComponent] = useUpdateComponentMutation({
     fixedCacheKey: selectedComponentId,
   });
@@ -80,7 +81,7 @@
     const default_values: DefaultValues = propData?.default_values || {};
     if (isEvaluatedComponentModel(selectedModel)) {
       patchComponent({
-        type: EditorFrameContext.TEMPLATE,
+        type: editorFrameContext,
         componentInstanceUuid: selectedComponentId,
         componentType: `${selectedComponentType}@${version}`,
         model: {
diff -ruN a/ui/src/components/panel/ContextualPanel.tsx b/ui/src/components/panel/ContextualPanel.tsx
--- a/ui/src/components/panel/ContextualPanel.tsx	2026-04-02 06:14:28.441936741 +0300
+++ b/ui/src/components/panel/ContextualPanel.tsx	2026-04-02 06:16:23.302955328 +0300
@@ -24,6 +24,7 @@
   selectSelection,
 } from '@/features/ui/uiSlice';
 import useHidePanelClasses from '@/hooks/useHidePanelClasses';
+import { getCanvasSettings } from '@/utils/drupal-globals';
 
 import type React from 'react';
 
@@ -38,6 +39,9 @@
   const isTemplateContext = editorFrameContext === EditorFrameContext.TEMPLATE;
   const mainTabText = isTemplateContext ? 'Template data' : 'Page data';
 
+  const hidePageDataPanel =
+    getCanvasSettings()?.hidePageDataPanel === true ||
+    /\/canvas\/editor\/node\/\d+/.test(window.location.pathname);
   const [activePanel, setActivePanel] = useState('pageData');
   const offRightClasses = useHidePanelClasses('right');
   const [hidePanel, setHidePanel] = useState(false);
@@ -59,12 +63,16 @@
   }, [dispatch, selectedComponent, isMultiSelect]);
 
   useEffect(() => {
-    if (isTemplateContext && !isMultiSelect && !selectedComponent) {
+    if (
+      (isTemplateContext || hidePageDataPanel) &&
+      !isMultiSelect &&
+      !selectedComponent
+    ) {
       setHidePanel(true);
     } else {
       setHidePanel(false);
     }
-  }, [selectedComponent, isMultiSelect, isTemplateContext]);
+  }, [selectedComponent, isMultiSelect, isTemplateContext, hidePageDataPanel]);
 
   return (
     <Box
@@ -90,7 +98,7 @@
             className={clsx(styles.tabRoot)}
           >
             <Tabs.List justify="start" mx="4" size="1">
-              {!isTemplateContext && (
+              {!isTemplateContext && !hidePageDataPanel && (
                 <Tabs.Trigger
                   value="pageData"
                   data-testid="canvas-contextual-panel--page-data"
@@ -169,7 +177,7 @@
                     <Outlet />
                   </ErrorBoundary>
                 </Tabs.Content>
-                {!isTemplateContext && (
+                {!isTemplateContext && !hidePageDataPanel && (
                   <Tabs.Content
                     value={'pageData'}
                     forceMount={true}
diff -ruN a/ui/src/components/topbar/Topbar.tsx b/ui/src/components/topbar/Topbar.tsx
--- a/ui/src/components/topbar/Topbar.tsx	2026-04-02 06:14:28.445048693 +0300
+++ b/ui/src/components/topbar/Topbar.tsx	2026-04-02 06:16:32.511101332 +0300
@@ -100,7 +100,7 @@
                 <DropIcon
                   className={styles.drupalLogo}
                   height="24"
-                  width="auto"
+                  width="24"
                 />
               </a>
             </Tooltip>
diff -ruN a/ui/src/components/UndoRedo.tsx b/ui/src/components/UndoRedo.tsx
--- a/ui/src/components/UndoRedo.tsx	2026-04-02 06:14:28.432696909 +0300
+++ b/ui/src/components/UndoRedo.tsx	2026-04-02 06:15:31.999146262 +0300
@@ -65,7 +65,7 @@
         disabled={!isUndoable}
         aria-label="Undo"
       >
-        <ResetIcon height="16" width="auto" />
+        <ResetIcon height="16" width="16" />
       </Button>
       <Button
         variant="ghost"
@@ -78,7 +78,7 @@
       >
         <ResetIcon
           height="16"
-          width="auto"
+          width="16"
           style={{ transform: 'scaleX(-1)' }}
         />
       </Button>
diff -ruN a/ui/src/features/editor/Editor.tsx b/ui/src/features/editor/Editor.tsx
--- a/ui/src/features/editor/Editor.tsx	2026-04-02 06:14:28.448198803 +0300
+++ b/ui/src/features/editor/Editor.tsx	2026-04-02 06:16:47.965346887 +0300
@@ -27,6 +27,7 @@
 import useEditorNavigation from '@/hooks/useEditorNavigation';
 import useReturnableLocation from '@/hooks/useReturnableLocation';
 import { useUndoRedo } from '@/hooks/useUndoRedo';
+import { getCanvasSettings } from '@/utils/drupal-globals';
 import { getLayoutItem, setLayoutItem } from '@/utils/layoutStorage';
 
 import styles from '@/features/editor/Editor.module.css';
@@ -88,10 +89,14 @@
   const { entityId, entityType, bundle, viewMode } = useParams();
   const { navigateToTemplateEditor } = useEditorNavigation();
 
+  const hidePageDataPanel =
+    getCanvasSettings()?.hidePageDataPanel === true ||
+    /\/canvas\/editor\/node\/\d+/.test(window.location.pathname);
   const isTemplateContext = editorFrameContext === EditorFrameContext.TEMPLATE;
   const isPanelHidden =
     editorFrameMode === EditorFrameMode.INTERACTIVE ||
-    (isTemplateContext && !isMultiSelect && !selectedComponent);
+    (isTemplateContext && !isMultiSelect && !selectedComponent) ||
+    (hidePageDataPanel && !isMultiSelect && !selectedComponent);
 
   const handleSidebarResize = useCallback((sizes: number[]) => {
     if (sizes.length === 2) {
diff -ruN a/ui/src/features/layout/layoutModelSlice.ts b/ui/src/features/layout/layoutModelSlice.ts
--- a/ui/src/features/layout/layoutModelSlice.ts	2026-04-02 06:14:28.451853288 +0300
+++ b/ui/src/features/layout/layoutModelSlice.ts	2026-04-02 06:16:55.308463785 +0300
@@ -768,8 +768,9 @@
       // Do not provide a resolved value with the newly linked prop.
       delete resolved[propName];
 
+      const editorFrameContext = selectEditorFrameContext(state);
       const valuePayload = {
-        type: 'template' as EditorFrameContext,
+        type: editorFrameContext,
         componentInstanceUuid: componentToUpdateId,
         componentType: `${selectedComponentType}@${version}`,
         model: {
